{
  "identifier": "default",
  "description": "Default capabilities for the app",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "core:webview:allow-print",
    "shell:allow-open"
  ]
}
